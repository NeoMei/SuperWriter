# 继续未迁移的旧项目

先把已安装 SuperWriter skill 根目录设为 `SUPERWRITER_SKILL_ROOT`，然后读该根目录下的 `references/legacy-v1/SKILL.md`。该文件保留旧流程原文，不能直接按它的相对路径读取新版契约。本入口的映射是旧项目所有引用的解析规则；下表两列路径均以安装 skill 根目录为基准，不以本文件所在目录或客户目录为基准。

| 冻结 SKILL.md 中的引用 | 实际读取路径 |
|---|---|
| `references/阶段契约.json` | `references/legacy-v1/阶段契约.json` |
| `references/门禁清单.md` | `references/legacy-v1/门禁清单.md` |
| `references/验收清单模板.json` | `references/legacy-v1/验收清单模板.json` |
| `references/依赖清单.json` | `references/依赖清单.json` |
| `references/响应策略表.md` | `references/响应策略表.md` |
| `references/应答矩阵模板.md` | `references/应答矩阵模板.md` |
| `references/素材打标规范.md` | `references/素材打标规范.md` |

前三项是冻结的 v1 执行契约、门禁和验收模板；其余引用继续使用根目录的共享参考文件。共享模板中的新版阶段提示不覆盖冻结契约：旧项目仍按阶段 0–9、门 0/2/3/5/6/7/8、仅门 2/5/8 人工确认执行，导出为交付验收。脚本和工具仍从安装 skill 根目录调用；客户产物仅写入当前客户工作区。

继续旧项目不初始化 v2、不生成新的历史批准。只有用户明确要求迁移时，才按新版入口先展示 `migration-preview`，取得明确迁移决定后执行 `migrate`。冻结的四份文件及 `source.json` 不修改。
