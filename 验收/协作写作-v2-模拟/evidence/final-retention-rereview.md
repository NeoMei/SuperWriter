# Selective retention integration rereview

## Verdict

**Specification: Approved. Code quality: Approved.** The known collection-only / retained-outline regression is **ADDRESSED**. No remaining concrete finding in this two-file fix or its interactions examined here.

## Exact reproduction and preservation

I reran the exact runnable public `apply_event` sequence from `final-rereview.md` entirely in memory. It now accepts the outline v2 approval with both unchanged chapters retained. The resulting statuses are approach/outline/c1/c2 approved, `fig1` still draft, and `figure-set` approved. Invalidations are empty; no individual figure approval event exists; `next_action` remains draft manuscript. This confirms the original rejection is fixed without inventing user approval.

The implementation now discharges the specific outline-change cause separately from status restoration. This is the correct distinction: removing a resolved cause must not leave descendants with provenance pointing to a cause already removed, and resolving that cause alone must not restore approval over another unresolved cause. Restoration recovers draft/pending/rejected figure lifecycle from durable events and exact registered identity. Approved status still requires an effective approval record; collection coverage supplies the permitted dependency check without changing individual member approval history.

The added public-store tests cover full retention for draft and pending members, partial retention with a still-stale sibling and preserved causes, independent content drift, and withdrawn collection/rejected member behavior. These assertions exercise the relevant durability and invalidation boundaries rather than only the helper's return value. Existing selective-retention/provenance tests remain passing.

## Validation evidence

- Exact former failing reducer probe: **PASS**, independently replayed during this rereview; no filesystem mutation.
- Frozen two-file digests match `final-retention-source-freeze.json`.
- Patch SHA-256: `729675f1bdd5ac8a8436107936e906ed1b43db686a8dae2d6bd579f740e5061c`.
- Fresh logs read: focused **10 PASS**, existing workflow **21 PASS**, full Python **168 PASS** in 83.790 s.
- Current-source native acceptance log: **PASS** for durable `验收/协作写作-v2-模拟`; fix report records unchanged state/manifest bytes and revision 63. Native artifacts were not regenerated by this fix.
- No broad suite rerun, new whole-branch search, production/index/branch/state edit, or subagent. Only this requested report was written. Parent owns final isolated install/artifact completion and final user-facing delivery claims.

Together with `final-rereview.md`, all four original whole-branch findings and the subsequent known regression are now addressed within reviewed scope. This verdict does not claim real-user approval, real-host installation, publication or deployment.
