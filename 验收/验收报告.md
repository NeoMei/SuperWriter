# superwriter 验收报告（模拟标段，2026-08-17）

## 结论：通过（交付级）

对照 spec §8：
- ✅ 全链 0→9 跑通（模拟 3 评分点技术标）
- ✅ 人工介入 = 3 次，且仅三类确认点（门 2 访谈共识 / 门 5 缺口补喂 / 门 8 终稿审定）
- ✅ 矩阵覆盖 3/3，无遗漏评分点；门 7 机器核查全过（术语一致、图引用、无残留占位）
- ✅ 门 0 仅做机器全覆盖核对；评分点总数抽查已并入门 2 客户合并确认，没有新增人工停点
- ✅ 阶段 6 由节点/边 spec 生成 Obsidian-native `.excalidraw.md`，PNG 视觉检查无截断、重叠或错连；章节和合并稿均引用渲染产物
- ✅ 导出验收使用 WPSComposer 原生 `overwrite=True` 生成 DOCX/PDF，无“门 9”且无旁路；DOCX 包结构校验通过并嵌入架构图，PDF 1.7 共 3 页且首页渲染正常
- ✅ `file` 正确识别 DOCX/PDF；markitdown 从两种格式均提取出正文、国产化双栈术语和图题
- ✅ 三宿主均从模拟客户目录调用完整源码 skill；三份旧镜像已可恢复隔离至 `~/.local/share/superwriter/backups/{agents,claude,codex}/`

## 非阻断环境备忘
1. PyPI 官方源在本机极慢，markitdown 装在专用 venv + PATH shim（~/.local/bin/markitdown）
2. npm 全局 markitdown 0.1.0 stub 是坏的（bin 自递归），已被 PATH 遮蔽；可择机卸载 npm 包
3. 本次 WPS 冷启动首次 `open -a` 遇一次 AppleEvent `-1712`；运行期端口与注册状态均正确清理，同一原生路径经隔离实例 `open -n` 重试成功。保留为非阻断运行环境观察项
